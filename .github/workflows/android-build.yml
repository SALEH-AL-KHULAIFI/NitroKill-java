name: Build NitroKill APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Build debug APK
        run: gradle assembleDebug --no-daemon

      - name: Build release APK (unsigned)
        run: gradle assembleRelease --no-daemon

      - name: Print APK size report
        run: |
          set -e
          RELEASE_APK=$(ls app/build/outputs/apk/release/*.apk | head -n1)
          DEBUG_APK=$(ls app/build/outputs/apk/debug/*.apk | head -n1)
          echo "== APK sizes =="
          ls -lh "$DEBUG_APK" "$RELEASE_APK"
          echo
          echo "== Release APK contents (size breakdown) =="
          unzip -l "$RELEASE_APK" | sort -k1 -n -r | head -n 25
          echo
          echo "== classes.dex / resources.arsc =="
          unzip -l "$RELEASE_APK" | grep -E "classes.*\.dex|resources\.arsc" || true

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: nitrokill-debug-apk
          path: app/build/outputs/apk/debug/*.apk

      - name: Upload release APK (unsigned)
        uses: actions/upload-artifact@v4
        with:
          name: nitrokill-release-apk-unsigned
          path: app/build/outputs/apk/release/*.apk
